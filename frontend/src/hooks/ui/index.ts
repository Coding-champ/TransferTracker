/**
 * UI Utility Hooks
 * 
 * Common UI interaction and state management hooks
 * for enhanced user experience and responsive design
 */

export { 
  useToggle, 
  useMultipleToggle, 
  useExpansion 
} from './useToggle';

export { 
  useLocalStorage, 
  useLocalStorageState
} from './useLocalStorage';

export { 
  useMediaQuery, 
  useMediaQueries, 
  useResponsive, 
  useUserPreferences, 
  useOrientation 
} from './useMediaQuery';